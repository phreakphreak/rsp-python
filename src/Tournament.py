


class Tournament:
    """
    Class that represents a tournament.
    """
    def __init__(self, name, players, rounds, matches):
        self.name = name
        self.players = players
        self.rounds = rounds
        self.matches = matches